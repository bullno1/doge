#!/bin/sh -e

cmd/linux/build || true

inotifywait -r -e CLOSE_WRITE -m shibe doge tests deps | \
    while read path action file
	do
		clear
		echo "Rebuilding"
		cmd/linux/build && cmd/linux/run || true
		echo "Done"
    done
